<h1> <%= @user.username %> </h1>

<div id="problems">
  <% @user.problem_attempts.each do |attempt| %>
    <div class="attempt">
      <%= attempt.problem.title %> - <%= attempt.status %>
    </div>
  <% end %>
</div>
